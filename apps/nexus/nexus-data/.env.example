# Live data mode is intentionally disabled by default in config/sources.yml
# Set demo_mode: false before using official APIs.
SAM_API_KEY=
# Optional, explicitly opt in to LLM enrichment; otherwise deterministic labels are used.
NEXUS_ENABLE_LLM=false
NEXUS_LLM_MAX_ROWS=50
OPENAI_API_KEY=
OPENAI_API_BASE=
