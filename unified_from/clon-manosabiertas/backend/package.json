{
  "name": "manosabiertas-leads-api",
  "version": "2.0.0",
  "description": "Lead capture server for ManosAbiertas",
  "main": "lead-capture-server.js",
  "scripts": {
    "start": "node lead-capture-server.js",
    "dev": "nodemon lead-capture-server.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop leads-api",
    "pm2:logs": "pm2 logs leads-api",
    "pm2:save": "pm2 save && pm2 startup"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
